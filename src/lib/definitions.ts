type optionSchema = {
    value: string;
    label: string;
}

export type mockDataSchema = {
    id: number;
    title: string;
    salary: string;
    location: string;
    experience: string;
}

export type expOptionsSchema = optionSchema[]
